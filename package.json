{
  "name": "express-arduino-serial",
  "version": "1.0.0",
  "description": "A server application written in express (nodejs) which is reponsible for establishing a web socket communication with a client, receives a message - command from the client and pass it to the serial port. Receives a json data from serial port and pass it back to client using the socket connection",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Sahad and Arafat",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "serialport": "^12.0.0",
    "socket.io": "^4.7.1"
  }
}
